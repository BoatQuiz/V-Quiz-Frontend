export interface FormValues {
    username: string;
    password: string;
}

export interface LoginUserDto {
    Id: string;
    Username: string;
}

export interface RegisterUserDto {
    Id: string;
    Username: string;
}